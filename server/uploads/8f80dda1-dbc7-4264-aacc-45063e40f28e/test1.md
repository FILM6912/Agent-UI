ถ้าจะ **ยกเลิก commit ก่อนหน้าใน Git** มีหลายกรณี ขึ้นกับว่า push ไปแล้วหรือยัง 👇

---

## ✅ กรณี 1: ยังไม่ได้ `push`

### 🔹 ยกเลิก commit แต่เก็บไฟล์ไว้ (แก้แล้ว commit ใหม่)

```bash
git reset --soft HEAD~1
```

* ลบ commit ล่าสุด
* ไฟล์ยังอยู่ใน stage (ready to commit)

---

### 🔹 ยกเลิก commit และเอาออกจาก stage (แต่ยังเก็บไฟล์ที่แก้ไว้)

```bash
git reset --mixed HEAD~1
```

หรือ

```bash
git reset HEAD~1
```

* ลบ commit
* ไฟล์ยังอยู่ แต่ไม่อยู่ใน stage

---

### 🔹 ลบ commit และลบไฟล์ที่แก้ทั้งหมด (ย้อนกลับจริง ๆ)

```bash
git reset --hard HEAD~1
```

⚠️ อันนี้จะลบการแก้ไขทั้งหมด หายถาวร

---

## ✅ กรณี 2: push ไปแล้ว

### 🔹 ถ้าอยาก “ย้อนกลับ” แบบปลอดภัย (แนะนำ)

```bash
git revert HEAD
```

* สร้าง commit ใหม่เพื่อแก้สิ่งที่ commit ก่อนหน้า
* ปลอดภัยกับ repo ที่มีหลายคนใช้

---

### 🔹 ถ้าจะลบ commit ที่ push ไปแล้วจริง ๆ (ระวังมาก)

```bash
git reset --hard HEAD~1
git push --force
```

⚠️ อันนี้จะ rewrite history
ถ้ามีคนอื่น pull ไปแล้วจะมีปัญหา

---

## 🎯 สรุปเลือกแบบไหนดี

| สถานการณ์                 | คำสั่งแนะนำ                     |
| ------------------------- | ------------------------------- |
| ยังไม่ push               | `git reset --soft HEAD~1`       |
| push แล้ว (ทำงานเป็นทีม)  | `git revert HEAD`               |
| ทำคนเดียว และต้องลบจริง ๆ | `git push --force` (หลัง reset) |

---

ถ้าบอกเพิ่มว่า:

* push ไปหรือยัง
* ต้องการเก็บไฟล์ไว้ไหม

ผมจะบอกคำสั่งที่ตรงที่สุดให้เลย 👍
