# ตั้งค่าว่าปุ่ม (copy, regenerate, edit, version) จะแสดงเมื่อ hover หรือแสดงตลอดเวลา
# true = แสดงเมื่อ hover เท่านั้น (ค่าเริ่มต้น)
# false = แสดงตลอดเวลา
# VITE_ENABLE_HOVER=true
# VITE_ENABLE_HOVER=true